# bedrock-deterministic-planner-poc
A minimal POC showing how to replace Bedrock Agents’ non-deterministic planning with a static, reproducible execution path.Uses YAML-based plans + deterministic step runner to avoid workflow drift. Drop-in, auditable, enterprise-safe.
